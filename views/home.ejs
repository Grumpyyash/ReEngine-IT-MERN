<%- include("partials/header"); -%>

<div class="home-header">
  <div class="row cards-row">

    <% personSell.forEach(function(item, index){ %>

    <div class="col-lg-3 col-sm-12">
      <div class="card" style="width: 18rem; text-align: center;">
      <img class="card-img-top" style="width: 100%; height: 200px; margin: auto;" src="<%= item.image %>">
      <div class="card-body">
      <h5 class="card-title"><%=item.itemName%></h5>
      <p class="card-category">catgory: <%= item.category %></p>
      <h5 class="card-text">₹ <%= item.price %> only</h5>
      <a href="/products/<%=item.itemName%>" class="btn btn-primary">Show More</a>
      </div>
     </div>
  </div>
  <% }) %>
  </div>
  </div>

<%- include("partials/footer"); -%>
